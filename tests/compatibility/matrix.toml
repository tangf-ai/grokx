# App ↔ engine compatibility matrix (expand as releases ship).

[[entry]]
app = "0.1.0"
engine_commit = "8cf69e3"
status = "scaffold"
notes = "Initial monorepo with subtree import of xai-org/grok-build; ACP loop not wired yet."
